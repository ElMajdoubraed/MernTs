// navbar feha change language login or logout buttons mawjoda fil les pages l kol kn auth layout
// landing page if ! auth
// dashboard if auth

const HomePage = () => {
  return <></>;
};

export default HomePage;
